machine:
    java:
        version: oraclejdk8
        
dependencies:
    pre:
        - ( sleep 5 && while [ 1 ]; do sleep 1; echo y; done ) | android update sdk --no-ui --all --filter extra-google-m2repository,extra-android-support,tools,platform-tools,build-tools,android-24
