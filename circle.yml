machine:
    java:
        version: oraclejdk8
        
dependencies:
    pre:
        - echo y | android update sdk --no-ui --all --filter extra-google-m2repository,extra-android-support,tools,platform-tools,build-tools,android-24
